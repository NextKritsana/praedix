---
title: "22강_.Net and COM_v1.2"
type: "acs-advance-pdf"
course: "ACS Advanced"
course_folder: "Incident Response"
source_path: "E:\\ProJect\\ACS File\\advance\\Incident Response\\22강_.Net and COM_v1.2.pdf"
source_size_bytes: 772710
source_modified: 2025-11-12T12:46:03
imported_at: 2026-06-14T14:26:38
tags:
  - acs
  - acs-advanced
  - imported
---

# 22강_.Net and COM_v1.2

- Source: [22강_.Net and COM_v1.2.pdf](file:///E:/ProJect/ACS%20File/advance/Incident%20Response/22%EA%B0%95_.Net%20and%20COM_v1.2.pdf)

> Imported from PDF for Obsidian search. Verify formatting against the original file when exact layout matters.

## Page 1

.Net and COM
• What is .NET?
• Try .NET
• Get a taste of COM
22
1

## Page 2

What is .NET?01
cmdlet
OP
function
If, for
module
cmdlet:
Basic commands provided
by Powershell
1
Operator
Output with different
operators
2
If, for
Use conditional statements and
loops to perform a variety of
functions and improve efficiency
3
Function
Improve reusability of code with
functions
4
module
Packages of functions, cmdlets,
variables, and more
5
2

## Page 3

What is .NET?01
Once again, what is Powershell?
Powerful command line interface and scripting language developed by Microsoft
Based on the .NET Framework to perform system administration, automation, and scripting tasks on Windows
operating systems
Powershell adopts an object-oriented approach to treating
data as objects, making it easy to filter and manipulate data
Object Oriented
Fully integrated with the .NET Framework, compatible with
the .NET language, enabling direct calls to and use of .NET
assemblies and classes
.Net Framework Integration
NO
 compiler
Build code with conditional
statements, loops,
functions, and more
Coding
Runs without a separate
compiler
Compile
Do
Coding
3

## Page 4

What is .NET?01
High like the moon rock with me baby
Know that I got that heat
Win32 API
.NET
• .NET is a software development framework developed by
Microsoft that supports languages such as C# and Visual
Basic and provides a rich class library and execution
environment
• Powershell is fully integrated with the .NET Framework,
so it is compatible with the .NET language and can call
and use .NET assemblies and classes directly
.NET?
How to use it
.NET technologies can be leveraged to perform server administration, automation, scripting, development
tasks, and more
However, malware that uses Powershell and .NET can be leveraged for a variety of attacks and intrusion
techniques 4

## Page 5

What is .NET?01
https://www.javatpoint.com/vb-net-dot-net-framework-introduction
VB C C# JS ...
Common Language Specification
Data and XML
Base Class Library
Common Language Runtime
Windows COM+ Services
ASP.NET Windows Form
Visual
Sutdio
.NET
➢.Net Framework
• A software development platform developed by Microsoft that provides a
comprehensive environment for developing and running various kinds of applications
on the Windows operating system
• It is characterized by its multi-language support, allowing development using a variety
of languages such as C#, VB.NET, F#, etc.
• Similar structure to the JAVA framework
• Short for Common Language Runtime, an execution environment that
plays a key role in the Microsoft .NET framework
• Provides core services such as memory allocation and deallocation,
exception handling, threading, etc.
CLR
• Provides the classes and methods you need to perform
common tasks, enabling you to develop applications effectively
• Support for classes like System.IO, System.NET, and
System.Collections
BCL
• Short for Common Type System, a standard for defining data types
and metadata used in the .NET Framework
• Defines basic data types such as Int32, Double, String, etc.
CTS
• Short for Common Intermediate Language, used as an intermediate
language in the .Net Framework
• Bytecode that is generated when source code written in various .NET
languages is compiled
• High-level language that represents code in a form that is independent of
the computer's hardware architecture, typically written in assembly
language
CIL
5

## Page 6

What is .NET?01
.NET
Assembly
Assembly
Class
Method
DLLs with additional data in the form of a manifest
A package of executable code and related resources for the .NET
framework
One or more classes and their associated data, metadata,
and resources, assembly-level instructions, etc.
A combination of code and metadata
• Metadata holds information such as the version of the assembly, dependencies, format
definitions, etc.
• Allows assemblies to be managed as specific versions and run in different environments
Combinations of modules
• An assembly contains one or more modules, where a module is a single source code file
written in the .NET language, and the combination of modules is called an assembly
Assemblies
• Assemblies are language neutral and can be loaded and unloaded as needed
Attributes
6

## Page 7

Try .NET02
Static Method
[Class]::MethodName()
 [System.Math]::Max(5, 8)
Instance Method
$var = New-Object CLASS
[Assembly.Class]::Method
$now = Get-Date
$year = $now.Year
$month = $now.Month
Write-Host "Current Year: $year"
Write-Host "Current Month: $month"
• Methods That Belong to a Class, Not an Instance of a Specific
Class
• In PowerShell, you use :: to access the static methods of a class
• Leverage existing .NET classes and methods to perform powerful
scripting and automation tasks
Static methods
• Instance methods work on a specific state of a specific
object and can only be called after you create an instance of
that object
• Create an instance of that class first, and then use . (dot) to
access the method
Instance Method
7

## Page 8

Try .NET02
[System.Diagnostics.Process] | get-member -static
Command
[System.Diagnostics.Process]::Start("calc.exe")
Command
• The System.Diagnostics.Process class is a class provided by the .NET
Framework that provides the ability to start and control external
processes
• Used to start a process, get properties of the process, and interact
with the process
• You can use get-member -static to determine which static methods
are available to you
Description
• Using the Start method in a static method
• Confirm that Notepad is running
Start
8

## Page 9

Try .NET02
• A class provided by the .NET Framework that provides simple
access to web resources such as HTTP, FTP, and file
downloads and uploads
• These classes are used to send or receive data to and from
web resources through a simple API
• Methods such as DownloadString, DownloadData,
UploadString, and UploadData can be used to download or
upload data to an HTTP or FTP resource
System.Net.WebClient
In addition to these...
HTTP request headers can be set via the Headers property and network credentials via the Credentials property
The DownloadProgressChanged and DownloadFileCompleted events allow you to handle download progress and post-completion
actions
The WebClient class is easy to use and provides a wide range of functionality to facilitate simple communication with web resources
9

## Page 10

Try .NET02
Command
Result
• Example of getting the String of a https://google.com
via DownloadString
• Get the Strings that are actually visible when you turn
on developer mode on that webpage
Description.
10

## Page 11

Try .NET02
Test Port
Connectivity
system.net.sockets.tcpclient
ASSEMBLY CLASS METHOD
• Provides the ability to establish and manage TCP network connections
• TCP is a protocol that supports reliable, connection-oriented
communication. TcpClient allows you to create TCP connections from client
applications to servers
System.Net.Sockets.TcpClient
• Can establish a TCP connection and get a NetworkStream object via the
GetStream method to send and receive data
• Close a TCP connection by calling the Close method and use
asynchronous methods such as BeginConnect and BeginRead to perform
asynchronous TCP operations
Key features
Example
Create a new instance of the system.Net.Sockets.TcpClient class, assign it to the $obj variable, and output the members of the $obj variable to the Name and MemberType methods
11

## Page 12

Try .NET02
Connecting Ports
Tests are run on localhost
Pass the result of Netstat -ano to findstr "127.0" using pipeline
Output the open ports on the localhost as a result
LISTENING Port : 6463
Check for open ports
Command
• Run $obj.connect("localhost", 6463)
• Check with $obj and see that the Connected value is changed to True
• We usually use telnet to test network connectivity, but you can use
PowerShell to perform a simple network connectivity test
12

## Page 13

Try .NET02
Not Open Port
• If that port is not open, the following warning is output
• This error is caused by the target server (127.0.0.1) not accepting connections on the
specific port (22356)
• Typically, this error occurs when the target server is not listening on that port or a firewall
is blocking the port
Error
13

## Page 14

Try .NET02
Port Scanning
• Port scanning is the process of checking the status of multiple ports for a computer or server on a network
• Through port scanning, an attacker can identify open ports on a target system and attempt to determine what services are running and
their versions, which can be used as a preliminary step in a vulnerability analysis and attack on the system
Prior knowledge
Methods for attempting asynchronous TCP connections
• Host is the hostname or IP address of the server to connect to
• Port is the port number of the server to connect to
• Requestcallback, the callback method that will be called after the
asynchronous connection completes
• AsyncCallback delegate type
Begin Connect
• Calling a Specific Method in PowerShell to Wait for an
Asynchronous Task to Complete
• Wait for an asynchronous task to complete for a specific
amount of time, and return a boolean value when the task
completes
AsyncWaitHandle.WaitOne
• Waiting for an asynchronous connection operation to complete and
then ignoring its results
• Enable when the asynchronous operation is complete after calling the
BeginConnect method
$var.Endconnect() | Out-Null
• Asynchronous connections are a programmatic way to wait for a
connection request to complete while performing other tasks, rather
than waiting synchronously for the connection request to complete
Asynchronous connections?
14

## Page 15

Try .NET02
function Test-Port {
    param (
        [string]$ComputerName = "localhost",
        [int]$StartPort,
        [int]$EndPort
    )
    for ($port = $StartPort; $port -le $EndPort; $port++) {
        $tcpclient = New-Object System.Net.Sockets.TcpClient
        $connection = $tcpclient.BeginConnect($ComputerName, $port, $null, $null)
        $result = $connection.AsyncWaitHandle.WaitOne(1000, $false)
        if ($result -eq $false) {
            Write-Host "Port $port is closed"
        } else {
            Write-Host "Port $port is open"
            $tcpclient.EndConnect($connection) | Out-Null
        }
        $tcpclient.Close()
    }
}
Test-Port
Set $ComputerName as the target for portscan with variables in Param,
$Stratport as the port to start, and $Endport as the port to end
Set $ComputerName to Localhost
Iterate over the variables starting at the Startport and ending at the EndPort
Create a TCP Clinet object
• The object will use the TcpClient method of the System.Net.Sockets
class to
Asynchronously attempts to connect to that
• The $connection variable stores an IAsyncResult object that represents
the asynchronous operation
Determine when the asynchronous operation is complete
• Uses the AsyncWaitHandle.WaitOne method to wait for the
asynchronous operation to complete for up to 1 second (1000
milliseconds)
• The $result variable stores $true if the operation is complete, or $false if
not
Output and finalization
• If $result is True, print Open and call EndConnect to ignore the return
value after the asynchronous connection operation completes
• Finally, at the end of the conditional statement, use Close to close the
TCP connection object 15

## Page 16

COM flavor03
COM Object
• Short for Component Object Model, a standard for portable binary interfaces of software components developed by Microsoft
• COM provides a technical standard that allows each component to be developed independently and used in combination with
other software
• Primarily used to implement object-oriented programming and service-oriented architecture in Windows environments
Based on binary interfaces
It's a standardized way for components to communicate with each other, providing interoperability across
different programming languages or environments
Adopt the concepts of object-oriented programming
Each component is represented by a COM object, and the object has its own interface, and the functionality of the object is accessible
through that interface
Providing Factory Methods
This allows clients to create instances of objects, access interfaces, and support service-oriented architectures that allow multiple components to interact and provide
services.
COM
16

## Page 17

What is .NET?01
$comObjects = Get-WmiObject Win32_COMSetting | Where-Object { $_.ProgID -ne $null }
foreach ($comObject in $comObjects) {
    Write-Host "ProgID: $($comObject.ProgID)"
}
COM Object Lookup
ProgID Output
• The COM object is assumed to be unversioned and interpreted as "first
version“
• ProgID is usually used with version information, but if no version is specified, it
is automatically resolved to point to the "first version"
What if there is no version information?
Since there is no command to output only the ProgID of a COM object, you can check the ProgID
using the script below.
COM flavor03
17

## Page 18

$excel = New-Object -ComObject Excel.Application
$workbook = $excel.Workbooks.Open("C:\Users\kusti\Desktop\make_ACS_test\ACSEXCEL.xlsx")
$worksheet = $workbook.Worksheets.Item(1)
$worksheet.Cells.Item(1, 1) = "Hello, PowerShell!"
$workbook.Save()
$excel.Quit()
Edit Execl
COM flavor03
Description
Create a new Excel application COM object, assigning the created Excel application object to the $excel variable
Open Excel file ("ACSEXCEL.xlsx") at the specified path using the Open method Open
Assigns an open workbook object to the $workbook variable
Assign the string "Hello, PowerShell!" to a cell in the first row, first column of the selected worksheet, save and
exit
As a result, the specified Excel file opens, with "Hello, PowerShell!" in the first cell
The Excel application exits after the changes are saved
Other possible lists
Word OUTLOOK
18

## Page 19

COM flavor03
$firewall = New-Object -com HNetCfg.FwMgr
firewallProfile = $firewall.LocalPolicy.CurrentProfile
firewallRules = $firewallProfile.GloballyOpenPorts
foreach ($rule in $firewallRules) {
    Write-Host "Port $($rule.Port): $($rule.Name)"
}
$firewallProfile
Firewall profile Foreach - rule
• COM object that manages Windows Firewall settings
• HNetCfg.FwMgr is used to access and manipulate Windows Firewall
settings programmatically
• H stands for Home Networking, indicating that this is a component that
deals with settings related to home networking, and FwMgr stands for
"Firewall Manager", indicating a component that manages firewall
settings
HNetCfg.FwMgr
Why?
When you run this script, it outputs a list of global ports for the current user profile and information about their rules
By using this script to view the global port list and rules for the current user profile, you can review the security configuration of your system and determine if there are any
unexpected rules or open ports
Useful for reviewing and debugging firewall rules when you encounter problems with them, especially when setting up network applications or services to ensure that ports are
opening properly
If a port is opened by an unknown application or service, this could be an early indication of an intrusion
19
